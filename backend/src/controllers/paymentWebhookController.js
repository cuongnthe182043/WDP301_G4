// DEPRECATED — VNPay and MoMo webhook handlers removed.
// PayPal does not use server-side webhooks for the capture flow;
// payment settlement is handled synchronously in paypalService.capturePayPalOrder.
